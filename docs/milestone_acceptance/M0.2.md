# M0.2 Acceptance Record

- Milestone: M0.2 — Binance-specific project identity correction
- Date: 2026-07-22
- M0 commit: `1634b09e57d287eba82ef34f117b4657979cc38b`
- M0.1 commit: `b186c08191392e7454d259d8cfd16d0263e0901f`
- M0.2 commit: the commit containing this record, with subject
  `chore: rename recorder for Binance market data`; resolve its SHA with
  `git log -1 --format=%H` and report it at handoff to avoid an impossible
  self-referential hash.
- Intermediate M0.1 identity: `Crypto Market Data Recorder`, repository
  directory `CryptoMarketDataRecorder`
- Final identity: **Binance Market Data Recorder**, repository directory
  `BinanceMarketDataRecorder`
- Former M0.1 workspace:
  `/Users/amada/Documents/Development/Crypto/CryptoMarketDataRecorder`
- Final workspace:
  `/Users/amada/Documents/Development/Crypto/BinanceMarketDataRecorder`
- Final distribution/import/CLI: `binance-market-data-recorder`,
  `binance_market_data_recorder`, `binance-market-recorder`
- Final macOS data directory:
  `~/Library/Application Support/BinanceMarketDataRecorder/`

## Identity and scope evidence

- [x] The same repository and `.git` history were moved; no repository was
  created and no commit was rewritten.
- [x] ADR-0007 supersedes ADR-0006, which remains intact historical evidence.
- [x] M0.1 acceptance is marked superseded without rewriting its facts.
- [x] Current documents use the final display, repository, distribution,
  import, CLI, and data-directory identities.
- [x] The project is scoped to Binance public market data with separate Spot
  and USD-M modules; other exchanges are not a V1 goal.
- [x] README, project contract, source policy, ADR-0007, and release plan state
  that the project is independent/unofficial and has no affiliation,
  maintenance, sponsorship, or endorsement relationship with Binance.
- [x] Binance logos, official visual identity, and Binance-owned-looking service
  namespaces are prohibited; the author's actual reverse-DNS namespace is not
  guessed.
- [x] M16 remains a generic consumer contract. Alpha101Crypto remains an
  optional external consumer and cannot influence Raw, Catalog, Manifest,
  Replay, or Recorder internals.
- [x] No M1 package/config implementation, `src`, `pyproject.toml`, Collector,
  WebSocket, GUI, storage service, production data, or Binance connection was
  added.

## Historical-name classification

The intermediate M0.1 identity/path is legal only in ADR-0006, the M0.1
acceptance record, ADR-0007, this M0.2 record, and split regression-test data.
The original M0 identity remains legal only in the already established
migration-history allowlist. No historical identity is a current package, CLI,
application data, service, README, contract, architecture, or usage example.

## Verification commands and results

- Git top-level:
  `/Users/amada/Documents/Development/Crypto/BinanceMarketDataRecorder`.
- Both historical object checks returned `commit` for M0 and M0.1.
- Intermediate workspace: absent. Final workspace `.git`: present.
- Intermediate-name searches: all remaining results are confined to ADR-0006,
  M0.1 acceptance, ADR-0007, and this record. Compared with the pre-correction
  inventory, net current references removed/replaced were: display name 8,
  directory token 13, distribution 4, import package 4, CLI 19, absolute
  workspace 3, and application-data path 8.
- Original M0 identity searches: four display/path tokens remain only in
  ADR-0006 and M0.1 acceptance; the three other requested spellings have zero
  results.
- Final identity searches found consistent display, repository, distribution,
  import, CLI, and data-directory uses across active contracts.
- Disclaimer/scope searches found the independent/unofficial/no-affiliation/
  no-maintenance/no-sponsorship/no-endorsement statements and Binance Spot/USD-M
  scope in README, project contract, ADR-0007, architecture, and release plan.
- Forbidden official-relationship wording and Binance-owned-looking reverse-DNS
  tokens: zero results.
- `python3.12 --version`: Python 3.12.9.
- `python3.12 -m pytest -q`: 1 passed.
- `python3.12 tests/verify_m0_contracts.py`: passed.
- `git diff --check`: passed with no output.
- No-production/M1/network/GUI/key scan: no `src`, `configs`, `pyproject.toml`,
  production Collector, WebSocket client, GUI/frontend, storage service,
  credentials, or Binance network implementation exists.
- External Alpha101Crypto status-only check: commit remained
  `22f9dae2955acc2d9d32e68b9d565bbfb185c8a5`; its pre-existing frontend
  modified/untracked files remained present. M0.2 did not modify or clean it.
- Post-commit status/log/HEAD and test rerun are required in the handoff report
  because they cannot be embedded before the containing commit exists.

## Known limitations and compatibility

M0.2 changes source workspace, documentation, and future identifiers only. It
does not create executable packages/services, migrate production data, or
change an implemented Raw/Catalog/Manifest/Replay format. M1 must implement the
final names, M3 must finalize the raw bytes, and M14 must choose an
author-controlled reverse-DNS namespace through an explicit decision.

Local editor/bookmark paths may need reopening after the directory move. M0 and
M0.1 remain directly reachable in the same Git history.
