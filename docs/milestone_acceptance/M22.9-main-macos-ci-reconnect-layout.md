# M22.9 main macOS CI reconnect-layout repair — acceptance

## Status and authority

`MILESTONE=M22_9_POST_MERGE_MACOS_CI_RECONNECT_LAYOUT_REPAIR`
`MILESTONE_STATUS=REVIEWED_COMPLETE`

This records the offline test-and-documentation closeout. Independent lead
review found P0=0, P1=0, and P2=0; the production/test delta is independently
reviewed complete, while GitHub merge remains a separate operation. It repairs a
schedule-sensitive test assumption in the Spot and USD-M session-restart
post-close timeout coverage. It does not change production code, public
contracts, Binance source records, dependencies, CI workflow, deployment,
VPS state, or archive data.

```text
BASE_MAIN_SHA=bed826909e1a53089289981e9ea435c7ddbf07f0
BASE_MAIN_TREE=a85a39f5096f834a95bae11f9d047fac18602bc6
BRANCH=codex/m22-9-main-macos-ci-reconnect-layout
GITHUB_MAIN_PUSH_RUN=34663566549
MACOS_PYTHON312_JOB=103470852952
UBUNTU_PYTHON312_JOB=103470853142
PR_68_EXACT_HEAD_RUN=34663191565
VPS_TOUCHED=NO
CURRENT_MAIN_DEPLOYED=NO
RECORDER=STOPPED
ARCHIVE_TIMER=ENABLED_ACTIVE
FORMAL_M22_9=EXECUTED_FAILED_AT_T0
FORMAL_M22_9_CREDIT_SECONDS=0
12H=NOT_STARTED
PRODUCTION_READY=NO
NEXT=EXACT_ARTIFACT_REDEPLOY_PREFLIGHT
REDEPLOY_RETRY_AUTHORIZATION=SEPARATE_AUTHORIZATION
```

`RECORDER=STOPPED` and `ARCHIVE_TIMER=ENABLED_ACTIVE` are the last
authoritative VPS values. This milestone did not connect to or mutate
the VPS, start Recorder, retry Formal M22.9, or touch production archive data.

## Diagnosis and implementation

The main push run's macOS pytest result was `1 failed, 1651 passed, 24
skipped, 4 deselected`. The failure was
`tests/integration/test_spot_ingress_backpressure.py::test_session_restart_post_close_timeout_recovers_same_gap_without_fabrication`
at the old line 794: `assert len(old_manifests) == 1`, while two manifests
were produced. PR #68 exact-head run `34663191565` passed both macOS and
Ubuntu; the main squash did not change this old test. Twenty local repetitions
of the exact Spot node on macOS arm64 Python 3.12.14 passed, confirming schedule
sensitivity rather than a deterministic product regression.

The existing `assert_old_ingress_boundary_layout` helper already describes the
valid second layout: at most one earlier ordinary complete manifest followed
by exactly one incomplete `reconnect_gap` manifest, with the original
connection identity and exact source payload prefix preserved. The neighboring
boundary-handoff timeout test already uses it. The Spot and USD-M
session-restart tests now use the same helper with the lifecycle STARTED
evidence's exact `original_connection_id`. Their boundary hash, missing-frame,
gap-id, recovery, completion-after-sync, ordering, no-fabrication, and
fail-closed assertions are unchanged.

## Verification ledger

- 20 complete repetitions of both repaired nodes: **40 passed**.
- Complete Spot and USD-M ingress backpressure files: **50 passed, 1
  deselected**.
- Full offline `python -m pytest -q`: **1,652 passed, 24 skipped, 4
  deselected, 13 existing warnings**, in 129.61 seconds.
- `python -m ruff check .`: **passed**.
- `python -m mypy`: **passed; no issues in 254 source files**.
- `python tests/verify_m0_contracts.py`: **passed**.
- `go run tools/verify_raw_chunk_golden.go`: **passed**.
- `python -m build --no-isolation`: **passed** in a Python 3.12.14
  validation environment after installing the missing build backend into the
  disposable venv; no lock file changed.
- Clean-wheel CLI smoke: **passed** in a fresh Python 3.12.14 environment;
  `--version`, `doctor`, and `status` succeeded. `doctor` reported no network
  access and no filesystem mutation.
- `git diff --check`: passed before final commit preparation.

The reusable test venv initially lacked `setuptools.build_meta`, so its first
build attempt failed before project packaging. The required build was then
rerun successfully in the isolated Python 3.12.14 environment after adding
only the backend package to that temporary environment.

## Scope exclusions and next gate

Online tests, stress/soak/manual platform runs, deployment, VPS commands,
archive operations, and manual CI dispatch/rerun/cancel were not run by this
milestone. After GitHub merge, the next milestone remains
`EXACT_ARTIFACT_REDEPLOY_PREFLIGHT`; this acceptance record is not a deployment
or retry authorization. Formal M22.9 remains failed at T0 with zero credit,
12h is not started, and Production Ready remains NO.
