# M20 Acceptance — Ubuntu ARM64 / RK3588

Status: **ACCEPTED — MERGED VIA PR #3**

Baseline:

- branch: `feat/m20-ubuntu-arm64-rk3588`
- `origin/main`: `43428a1b0784344c81b2ae0debf563dc30cdc13e`
- predecessor: M19.2 is present in `main`
- host: `opihome`, `192.168.0.118`, `aarch64`,
  `6.1.43-rockchip-rk3588`

Final PR Head: `2ebb981ea956929467b6dc4b0990875cc43e53bf`
Merge Commit: `80b8a5745fc64ee4e0ed0db7691c3acf7d2567bc`
PR: [#3](https://github.com/tomohikoAmada/BinanceMarketDataRecorder/pull/3)

## Scope conclusion

M20 adds only ADR-0025's unified transport policy and ADR-0026's Linux
paths/systemd/mounted-storage adapter. Spot/USD-M BTCUSDT scope, EventEnvelope,
Raw chunk, manifests, Catalog, normalized data, Historical clock semantics,
reconnect/resync/gap rules, and archive deletion authority are unchanged. No
credentials, account/order endpoints, trading, GUI/API server, additional
market, container platform, mount/format/repair, firewall, routing, or Mihomo
configuration change is included.

## Engineering gates

The final evidence record contains exact outcomes for:

- complete offline pytest and explicit stress;
- Ruff, strict MyPy, standalone contracts, Go Raw golden, build, and diff;
- final-Wheel install into a fresh aarch64 venv and imports for PyArrow,
  Zstandard, CRC32C, CBOR2, websockets, and both official Binance SDKs;
- Linux path, proxy modes/redaction/Mock CONNECT, systemd static/idempotence,
  mounted-storage fixtures, SIGTERM, restart, and macOS simulated regressions;
- direct and explicit public Spot/USD-M REST/WebSocket and small Historical;
- 30-minute systemd service smoke, safe stop/seal, start/recovery, restart;
- Mihomo restart with visible reconnect plus depth snapshot/resync or explicit
  unreliable interval evidence.

The physical Linux external disk exercise and real macOS platform run were
unavailable and remain explicitly unexecuted. M20 did not run or claim
72-hour/168-hour acceptance.

## Platform status

- macOS Apple Silicon: prior Developer Preview behavior retained; real macOS
  M20 regression not executed on RK3588.
- Ubuntu ARM64/RK3588: Developer Preview / Soak Candidate only.
- Production Ready, Trading Ready, zero-interruption, and long-run claims:
  forbidden.

## Rollback

SIGTERM stop and seal; retain `/var/lib/binance-market-data-recorder`; install
the previous Wheel; refresh the managed unit; restore the prior config; start
and require readiness. Never delete/modify Raw, Catalog, or an external
filesystem.

## Post-merge handoff

PR #3 was opened, Code Reviewed through two rounds, and merged. Code Review
identified and fixed four issues:

- systemd managed-unit verification across all lifecycle methods
  (install/start/stop/restart/uninstall);
- cross-platform systemd unit tests that failed on macOS;
- missing Ubuntu Python 3.12 CI job;
- Linux storage discovery falsely requiring RM/HOTPLUG flags.

macOS Python 3.12 CI passed (1m6s). Ubuntu Python 3.12 CI passed (1m37s).
All engineering gates are definitively closed. The production deployment on
RK3588 runs the merge-commit Wheel.

M20 is closed. M21 has not started.
