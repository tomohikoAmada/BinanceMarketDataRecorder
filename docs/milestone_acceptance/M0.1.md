# M0.1 Acceptance Record

Status: Superseded by M0.2. The facts below are retained unchanged as
historical evidence of the intermediate identity and workspace.

- Milestone: M0.1 — Project identity, workspace, and generic consumer boundary
  correction
- Date: 2026-07-22
- Previous milestone: M0, accepted at
  `1634b09e57d287eba82ef34f117b4657979cc38b`
- Former display identity: `Alpha101CryptoRecorder`
- Current display identity: **Crypto Market Data Recorder**
- Former workspace:
  `/Users/amada/Documents/Development/Alpha101/Alpha101CryptoRecorder`
- Current workspace:
  `/Users/amada/Documents/Development/Crypto/CryptoMarketDataRecorder`
- Distribution/import/CLI: `crypto-market-data-recorder`,
  `crypto_market_data_recorder`, `crypto-market-recorder`
- M0.1 commit: the commit containing this record, with subject
  `chore: rename market data recorder and relocate workspace`; resolve its SHA
  using `git log -1 --format=%H` and report it at handoff to avoid an impossible
  self-referential commit hash.

## Scope evidence

- [x] The same repository directory and `.git` history were moved; no new Git
  repository or history rewrite was created.
- [x] ADR-0006 freezes all current identities and explains the correction.
- [x] Current project, storage, CLI, service, and workspace contracts use the
  new identity.
- [x] Binance is an adapter; Raw, Catalog, normalization, replay, archive, and
  consumer contracts are generic.
- [x] M0.1 was inserted between M0 and M1; M16 and M18 use generic consumers.
- [x] Alpha101Crypto references are limited to historical read-only audit,
  optional ordinary consumer examples, and explicit no-reverse-dependency
  boundaries.
- [x] No M1 package/config implementation, `src`, Collector, WebSocket, GUI, or
  Binance connection was added.
- [x] The external Alpha101Crypto repository was only status-checked and not
  modified or cleaned.

## Legacy-name classification

Exact former Recorder identity/path text is intentionally retained only in this
acceptance record and ADR-0006 as migration evidence. It is not a current
project, package, CLI, data-directory, service, or launchd identity. The
contract verifier enforces this allowlist.

Other `Alpha101Crypto`/`Alpha101` references are legal only when they identify:

1. the external repository historically audited in M0;
2. an optional, ordinary external consumer example; or
3. the prohibition on importing/modifying or specializing the Recorder for
   that consumer.

## Verification commands and results

- Git top-level:
  `/Users/amada/Documents/Development/Crypto/CryptoMarketDataRecorder`.
- `git cat-file -t 1634b09e57d287eba82ef34f117b4657979cc38b`:
  `commit`; M0 history is reachable.
- Old workspace: absent. New workspace `.git`: present.
- Exact legacy identity/path searches: the 11 original current-identity
  occurrences were replaced. Four former display-name tokens and two former
  absolute paths remain only in this record and ADR-0006 as migration evidence;
  all other legacy spellings have zero results.
- CLI search: all 15 former generic-placeholder command tokens were replaced;
  zero remain. `crypto-market-recorder` occurs in current contracts/tests.
- All Alpha references were reviewed and fall into the three legal categories
  in the preceding section. The verifier source constructs legacy search
  fixtures from split strings and enforces file allowlists; it is not a project
  identity.
- `python3.12 --version`: Python 3.12.9.
- `python3.12 -m pytest -q`: 1 passed.
- `python3.12 tests/verify_m0_contracts.py`: passed.
- `git diff --check`: passed with no output.
- No-production/M1/network/GUI scan: no `src`, `configs`, production Collector,
  WebSocket client, GUI/frontend, pyproject/package skeleton, or Binance network
  implementation exists.
- External Alpha101Crypto status-only verification: commit remained
  `22f9dae2955acc2d9d32e68b9d565bbfb185c8a5`; its pre-existing frontend
  modified/untracked files remained present. No file was modified or cleaned by
  M0.1.
- Post-commit status/log/HEAD and test rerun are required in the handoff report
  because they cannot be embedded before the containing commit exists.

## Known limitations and compatibility

M0.1 changes documentation, repository location, and frozen future identifiers
only. It creates no distribution/import package/CLI/service, migrates no
production data, and changes no executable Raw/Catalog/replay format. M1 must
implement the new names; the byte-level raw format remains an M3 task.

The repository move may require local editor/bookmark paths to be reopened.
The original M0 commit remains directly reachable in the same history.
