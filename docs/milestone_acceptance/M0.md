# M0 Acceptance Record

- Milestone: M0 — Repository audit, boundary freeze, and complete plan
- Date: 2026-07-22
- Predecessor: none
- Bootstrap exception: target repository/`AGENTS.md`/plan did not exist before
  M0; Alpha101Crypto contained no applicable `AGENTS.md`.
- Scope status: documentation and tests only; no production package, CLI,
  network client, SDK dependency, Collector, GUI, or research-repository edit.

## Evidence checklist

- [x] Required Alpha101Crypto contracts, pyproject and Binance
  data/exchange/store modules were inspected read-only.
- [x] Existing dirty Alpha101Crypto frontend worktree state was recorded and
  preserved.
- [x] Independent Recorder Git repository created at the requested path.
- [x] Project, architecture, data, storage and macOS boundaries frozen.
- [x] Raw-format, repository, archive, clock and transport-gate ADRs accepted.
- [x] Every M0-M18 milestone specifies scope, non-scope, dependencies,
  acceptance and rollback.
- [x] Official Binance source URLs, retrieval date, SHA-256, SDK candidates,
  changelogs and confirmed planning semantics recorded.
- [x] Unknowns and platform/API risks explicitly assigned.
- [x] Charter requirement families mapped in traceability matrix.
- [x] Minimal standalone/pytest contract tests created.
- [x] Final pre-commit commands and results recorded below.
- [x] Local M0 commit is the single commit containing this acceptance record;
  its hash is resolved with `git log -1 --format=%H` and reported at handoff.
- [x] A post-commit clean-worktree check is required and reported at handoff;
  it cannot be embedded into the commit without changing that commit.

## Verification results

- `/Users/amada/miniforge3/bin/python3.12 -m pytest -q`: 1 passed.
- `/Users/amada/miniforge3/bin/python3.12 tests/verify_m0_contracts.py`:
  passed.
- `git diff --check`: passed with no output.
- Forbidden production artifacts scan: passed; no `src/`, `configs/`, or
  `tools/update_binance_docs.py` exists.
- Alpha101Crypto audit commit/status recheck: audited commit remained
  `22f9dae2955acc2d9d32e68b9d565bbfb185c8a5`; pre-existing frontend changes
  remained present and were not touched.
- Host default `python3 -m pytest -q`: not run successfully because the default
  Python 3.14.6 environment has no pytest. This did not lower the Python 3.12
  gate; the certified interpreter's pytest run above passed.

## Commit

The commit containing this file is the M0 local commit. Its concrete hash and
the post-commit clean-worktree check belong in the handoff report, avoiding a
self-referential commit hash. No M1 work is included.

## Compatibility impact

No production data format or Alpha101Crypto code changed. ADR-0002 establishes
the future raw format at a logical level; its executable byte specification
does not exist until M3.
