# M22.9-P2 — Exact deployment, archive, and capacity preflight (reviewed complete)

## Status and scope

`M22_9_P2=REVIEWED_COMPLETE`

This record covers one bounded, non-formal milestone: installation of the exact
P2 deployment source/review base artifact, verified archive operation, a short readiness/live
interaction, and capacity preflight. It is not a Formal M22.9 duration stage,
does not create a Formal T0, and does not make the project Production Ready.

```text
M22_9_P2=REVIEWED_COMPLETE
P2_EXACT_DEPLOYMENT_SOURCE_INSTALLED=YES
CURRENT_MAIN_DEPLOYED=NO
CURRENT_MAIN_DEPLOYMENT_REASON=DOCS_ONLY_DESCENDANT_NOT_INSTALLED
RECORDER=STOPPED
ARCHIVE_TIMER=ENABLED_ACTIVE
FORMAL_M22_9=NOT_STARTED
FORMAL_M22_9_CREDIT_SECONDS=0
PRODUCTION_READY=NO
NEXT=FORMAL_M22_9_2H_START_REQUIRES_SEPARATE_AUTHORIZATION
```

P1 remains `REVIEWED_COMPLETE`; no P1 stage observer was started. The original
MS4-C partial window and the non-formal MS4 recovery supplement remain
historical evidence and are not duration credit for this milestone.

## Authority and evidence

- P2 deployment source/review base: `646792f2e5fc5b7195ea58541d3f1dfda6555b7f`
- P2 deployment tree: `c7bcd5efbd9601e1dcef8c5e000435f2e0f82a6c`
- Evidence root: `/srv/recorder-data/recorder-archive/evidence/M22.9-P2-20260911T090741Z`
- `EVIDENCE_SUMMARY.md` SHA-256:
  `8104fd0375b17bd892a32797dbdaf2d1f56fd6007dcdd8c3a7c7d98eb92f6580`
- `SHA256SUMS` SHA-256:
  `80277b542fa320fe419f015e95ade52f380661dbf6c7bc314e40a1bfa85cf68d`
- Wheel SHA-256:
  `4bbe3c492533f26f06e0c224800f9954786bc4b605bf65d25a48d03bbaffb072`
- Lock SHA-256:
  `44cd373324f2af5f2682851996bc59a16199c65f8de9e98089131e1c67d6f335`
- Config SHA-256:
  `4dbbb6bf415b209635857df6cf537478b03f262b2c3a88553ac77bb9c1c0d781`
- Recorder unit SHA-256:
  `d5afc4c2228a78f02ffd7be07775e7c53acda90b8c2b1b3581d64020537188b6`
- Deployment identity SHA-256:
  `11029b9434f72fe48912659c050167e6a058e9827cd40401c9a852dff3c019cd`
- Registered archive storage: ID
  `ef852751-721c-4145-9083-f6fd48718480`, relative path
  `recorder-archive`, resolved at `/srv/recorder-data/recorder-archive`

The bundle contains command output, UTC timestamps, exit statuses, hashes, and
before/after state records. It contains no secret, account, order, key, or
credential material. Host-bound operational identifiers may remain only in the
private evidence bundle and are not copied into repository docs.

## Gate ledger

| Gate | Result | Direct evidence and limitation |
| --- | --- | --- |
| Exact source, clean tree, fresh wheel and lock | PASS | `01-source-checkout.*`, `02-build.*`, and the hashes above |
| Exact stopped deployment and four ProductKeys | PASS after correction | `03b-fixed-path-rebuild.*`, `04d-rollback-authority-success.*`, final deployment verification |
| Rollback compatibility | PASS; actual rollback NOT RUN | Old identity SHA `e925733f9e0388b705ffcad40664bdb049ba786f4b170462275891f838cd41e5`, root-controlled at `/etc/binance-market-data-recorder/deployment-identity.pre-p2-20260911T090741Z`; result `COMPATIBLE` |
| Official Spot/USD-M eligibility | PASS | Direct unsigned `exchangeInfo` responses; both symbols `TRADING` in both markets |
| Acceptance identity preflight | PASS | `10-acceptance-preflight/identity-result.json`, SHA `972ea1d509bcba0f629142f06d690034644e7ed8e079f4a6aa8fedc2bc506565` |
| Recorder readiness | PASS | Four ProductKeys and 12 core stream contexts READY; `readiness-result.json` SHA `e0088f3d2667da583bd33cb23625037e781744a8839a275ae17ba7b81ac0a102` |
| Archive canary | PASS after invocation correction | Chunk `8563ead1-40fd-47c1-aeba-d8d0952d61a4`, transaction `7b5b8a45-7a05-5f74-83eb-97c22830a183`, 20,721-byte readback/hash/manifest binding, terminal `LOCAL_DELETED` |
| Autonomous archive drain | PASS | Timer bootstrap followed by autonomous cycles; zero failed transactions and backlog reached zero |
| Short non-formal live interaction | PASS with bounded warnings | Stable service incarnation, four products READY, Catalog OK; snapshot-bridge pending warnings were followed by synchronized READY state |
| Graceful stop and post-stop drain | PASS | Service inactive/dead, PID 0, success result, no active partial, backlog zero |
| Full archive verification | PASS | Positional `archive verify` run: 112,570 verified, 0 failed, 0 pending; stdout SHA `b5cb4e8a944bf5024f90a4c8f4cf05371b460ac73b657c81e7e14b4a064ec22a` |
| Final Catalog, target, and deployment state | PASS | Catalog integrity OK, all local transactions terminal `LOCAL_DELETED`, remote pending 0, target READY, exact deployment verified |
| Capacity margin | PASS with active-root limitation | Archive margin covers the bounded projection; active-root runway is only forecasted and must be rechecked before every Formal stage |

## Deployment and rollback

The fresh venv was created directly at
`/opt/binance-market-data-recorder/venv` with `venv --copies`; it was not
renamed after installation. The exact lock was installed with hashes, the
wheel with `--no-deps`, `pip check` passed, dependencies matched, and CLI
version/config/doctor/status smoke checks passed. The fixed unit is root
controlled, non-root (`bmdr:bmdr`), has no unexpected drop-ins or environment
override, and retains the canonical four ProductKeys:

```text
spot: BTCUSDT, ETHUSDT
um_perpetual: BTCUSDT, ETHUSDT
```

The prior working venv remains at
`/opt/binance-market-data-recorder/venv-pre-p2-20260911T090741Z`; the broken
staging venv remains retained at its unique `venv-failed-p2-*` path. The prior
303e identity is retained at the root-controlled rollback locator above.
Rollback compatibility passed; no rollback was executed and no old release,
backup, Raw, or Catalog record was deleted.

## Eligibility, readiness, and interaction

Immediately before start, direct unsigned official public responses were
captured at 2026-09-11T09:49:16Z. Exact URLs, hashes, and conclusions are in
[`binance_sources.md`](../binance_sources.md): Spot and USD-M BTCUSDT/ETHUSDT
were all `TRADING` (USD-M contracts `PERPETUAL`). No account, order, key, or
credential endpoint was accessed.

The acceptance identity preflight used the stable child under the registered
archive evidence root. The Recorder then reached readiness within the bounded
300-second command: all four ProductKeys, 12 core stream contexts, startup
recovery, heartbeat/network, capacity, and identity checks were recorded. This
was preflight evidence only; no Formal T0 or duration credit was created.

The short interaction window ran from
`14-interaction/t0-20260911T095540Z` through
`poll-003-20260911T100109Z` (329 seconds). Active-root free space changed from
44,059,230,208 to 44,044,808,192 bytes, giving the required bounded measure
`max(0,(F0-F1)/dt) = 43835.914894 B/s`. This is a short shared-host
observation, not a long-run generation rate. No disconnect or recovery event
was observed in this window; pending snapshot-bridge warnings were followed by
synchronized READY state.

Recorder was then gracefully stopped. Final service state was inactive/dead,
`MainPID=0`, `Result=success`, `NRestarts=0`, with no active `.partial` file.
The timer drained the sealed post-stop backlog and remained enabled and
active/waiting with a future monotonic trigger.

## Archive and capacity

The canary froze the oldest unowned SEALED chunk before execution. The first
invocation failed before parsing because `bmdr` inherited `/root` as its
working directory; it created no transaction and did not mutate data. The
corrected `/var/tmp` invocation retired exactly one source through the existing
ArchiveManager/Catalog transaction after full external readback, size/SHA,
manifest binding, and atomic/authorized checks. No source was manually
removed.

The initial backlog was 1,742 files / 452,412,555 bytes. After the one-file
canary, the timer was installed with interval 60 seconds, maximum runtime 50
seconds, and maximum files 1,000, using the fixed venv and `bmdr` service. A
first explicit bounded service activation established the monotonic
`OnUnitActiveSec` schedule; subsequent autonomous cycles were observed. The
backlog reached zero with zero failed transactions. The final Catalog had
112,570 local archive transactions, all terminal `LOCAL_DELETED`, and zero
remote pending transactions.

The initial backlog-clearance envelope cleared 452,391,834 bytes from about
09:41:31Z to about 09:46:31Z, approximately 1,507,972.8 B/s wall-clock. The
service-active mean was 1,762,536.6 B/s. The full-window 347,072.9 B/s value
includes long zero-backlog idle cycles and is a workload average, not saturated
drain capacity.

The final storage forecast selected the conservative existing 24-hour
max-window median generation rate `349910.017730 B/s` (the 7-day selected rate
was lower). At 1,000,800 seconds, the 278-hour projection is
`350189945744` bytes. Archive free space was
`2121728036864` bytes, leaving approximately
`1771538091119.816` bytes of margin (about 6.06x the projection and 83.50%
remaining margin). The target was ext4 `/dev/vdb1` mounted at the registered
relative path and ended READY with no `.copying` or unexpected `.partial`.

The active writer filesystem `/dev/vda1` was
`61285326848` bytes total with `44067897344` bytes free at the forecast
observation. Above the 10 GiB hard reserve, this is only about 26.46 hours at
the conservative rate. Archive capacity therefore passes its bounded target
margin gate but does not prove long-run active-root runway. Every Formal stage
start and end must independently recheck timer status, backlog, target margin,
and active-root runway.

## Preserved incidents and unrun scope

The evidence bundle preserves all original outputs and corrections:

- The first deployment check found an absolute console-script shebang pointing
  to a renamed staging venv. A final-path direct rebuild passed before service
  start.
- The first unit assertion rejected valid `changed=false` managed bytes, and a
  rollback check first used an evidence parent that was not root controlled.
  The corrected root-controlled old identity passed `COMPATIBLE`.
- The first canary inherited `/root` as its cwd and failed before parsing; the
  corrected `/var/tmp` run passed with no preceding transaction.
- The first full verify used unsupported `--storage-id`; the corrected
  positional invocation passed.
- The initial acceptance evidence directory was root-created and inaccessible
  to `bmdr`; the stable owner-corrected root passed.

No Formal M22.9 stage, P1 observer, long soak, manual CI dispatch/re-run/cancel,
or account/order/key/credential endpoint was used. Full local pytest, Ruff,
MyPy, build, and manual CI were not run for this docs-only closeout. Deployment
pip checks, readiness, archive drain, full archive verification, and the
authorized VPS capacity forecast were run as P2 gates. P2 is reviewed complete;
the next named milestone is
`FORMAL_M22_9_2H_START_REQUIRES_SEPARATE_AUTHORIZATION`. No Formal stage is
started by this record.
