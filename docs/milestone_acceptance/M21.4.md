# M21.4 USD-M ingress backpressure and stream recovery

## Status

**IMPLEMENTED IN DRAFT PR / NOT DEPLOYED**

This is a code, incident-evidence, and deterministic offline-test milestone.
The Draft PR must remain open and unmerged for ChatGPT Code Review. Production
still runs the prior Wheel. No 2-hour, 12-hour, 24-hour, 72-hour, or 168-hour
validation has been restarted.

## Repair scope

- Replace USD-M WebSocket receipt `put_nowait` failure with bounded,
  Writer-aware, stop-aware awaitable backpressure.
- Close a continuously saturated connection at a finite deadline and give the
  already-received boundary frame a separate bounded post-close queue handoff.
  Raw drain/fsync/seal then safely follows the underlying I/O without a false
  five-second end-to-end persistence claim.
- Drain/seal before opening a new generation; never mix the old and new
  overload generations in one Raw chunk.
- Persist the existing `sequence_gap` flag on both the last-old boundary and
  first recovered Raw event, explicitly sync the first-new marker, and only
  then commit internal discontinuity completion evidence. Recover a unique
  unmatched STARTED record across process restart; conflicting unmatched gaps
  fail closed. Both sides remain conservatively incomplete.
- Recover `book_ticker` and `agg_trade` at stream scope without restarting the
  Recorder; historical completeness remains false.
- Force `diff_depth` through UNTRUSTED, fresh Snapshot, and correct `U/u/pu`
  bridging before READY.
- Preserve process-level fail-closed behavior for boundary-persistence,
  append, drain, fsync, seal, Catalog, and unexpected Writer failures.
- Add transition-only queue/Writer latency and generation diagnostics without
  changing formal soak or public schemas.

## Overflow contract by stream

| Stream | Sustained backpressure result |
|---|---|
| USD-M `diff_depth` | Close producer; persist boundary; seal generation; mark Raw gap; set readiness failure and request market-local resync; retire session; fresh connection + Snapshot + diff bridge required before READY. |
| USD-M `book_ticker` | Close producer; persist boundary; seal generation; durable gap event; reconnect only this stream; first new Raw event has `sequence_gap`; never claim historical continuity restored. |
| USD-M `agg_trade` | Same as `book_ticker`; trade loss cannot be reconstructed from a Snapshot. |
| USD-M mark price/liquidation | Same bounded transport primitive and persistent Raw gap marker; existing side-data supervisor remains isolated from core markets. |
| Writer or gap-evidence integrity failure | Fatal. Retain recoverable partial and propagate the original cause; do not continue receiving or report continuity. |

The post-close handoff timeout covers only admission of the already-received
frame to the bounded receipt queue. Raw drain, fsync, compression, seal, and
Catalog completion remain owned operations whose duration depends on safe
completion of the underlying I/O. Permanently hung kernel I/O remains a known
risk.

The existing Raw/Manifest/Catalog/Normalized/Replay schemas and public
consumer contract are unchanged. This patch reuses existing `sequence_gap`,
`gap=true`, and `complete=false` semantics. The two new operational event names
are internal additive evidence, not a schema change.

## Deterministic test gates

Tests cover:

- a burst larger than receipt capacity with exact ordered persistence, no
  loss/duplicate, bounded watermark, and no supervisor terminal path;
- sustained producer-over-consumer load with finite connection closure,
  persisted boundary frame, sealed generations, stable error types, and no
  deadlock;
- disconnect → reconnect → `book_ticker`/`agg_trade` burst → slow Writer;
- diff-depth overload, old-generation READY exclusion, clean bootstrap reset,
  fresh Snapshot, correct USD-M bridge, and READY only after bridge;
- Raw/Manifest/Catalog gap visibility for `book_ticker` and `agg_trade`;
- append, drain, fsync, seal, Catalog, and early Writer failures;
- stop/cancel during queue put, post-close put, WebSocket recv, Raw drain,
  reconnect backoff, planned rotation, and overflow handling;
- no pending Tasks, un-retrieved exceptions, queue-accounting duplicate, or
  rewritten persisted data;
- 1000 disconnect/reconnect/planned-rotation cycles and 100 overload boundary
  cycles, with bounded queue/FD/thread/Python-memory checks;
- recoverable stream overload remains below `MarketCollectorSupervisor`, while
  fatal storage/continuity failures still propagate;
- existing Spot, side-data, Recovery/Archive, multi-process, and global-stop
  behavior;
- an offline benchmark matrix for small messages, reconnect burst, slow
  append/fsync/seal, batch 8/64/256, and near-capacity operation.

Final local results and the Draft PR CI gate are:

- focused M21.4/market/storage suite: `136 passed in 42.17s`;
- 1000/100 long-cycle stress: `2 passed in 13.57s`;
- million-event stress: `1 passed in 196.51s`;
- default pytest: `587 passed, 28 skipped, 3 deselected in 71.15s`;
- Ruff: `PASS`;
- strict MyPy: `PASS`, 193 source files;
- M0 contracts: `PASS`;
- Go Raw golden: `PASS`;
- build and clean Python 3.12.13 non-editable Wheel install: `PASS`;
- final Wheel SHA-256:
  `d454f3d00c80f0e557cc2ab171df054e7cd749a71db49733b3043cddc4b922ff`;
- `git diff --check`: `PASS`;
- macOS CI: `PASS`, Draft PR run 30830392445, job 91742598005;
- Ubuntu CI: `PASS`, Draft PR run 30830392445, job 91742597955.

## M21.4.1 blocking-review correction

PR #7 review comment 5173209447 identified that a nonzero Raw durability
interval allowed Catalog completion to become durable before the first-new Raw
marker, and that an unmatched STARTED record was held only in process memory.
The correction preserves this strict order:

1. append and drain the first-new `sequence_gap` envelope;
2. explicitly `StreamSpool.sync()` that Raw marker;
3. commit `STREAM_DISCONTINUITY_COMPLETED` with SQLite `synchronous=FULL`;
4. only then notify the envelope observer/readiness path and clear recovery
   state.

Sync or Catalog failure leaves STARTED unmatched, propagates the original
exception, and remains Writer/Collector fatal. At construction, every USD-M
stream queries the existing operational-events table for its unmatched STARTED
records. Exactly one restores the same gap ID, original connection, and
original generation; the first accepted event in the replacement generation
gets one `sequence_gap` marker. Multiple unmatched records raise stable
`IngressGapStateConflict` without selecting one or writing new evidence. A
matched STARTED/COMPLETED pair starts normally without adding another marker.
No Catalog, Raw, Manifest, or public schema changed; only a read-only Catalog
query was added.

Deterministic tests use `durability_interval_seconds=0.75` and cover ordering,
sync failure, reconstructed Collector state, same-gap completion for
`book_ticker` and `agg_trade`, exactly-once marker placement within the resumed
generation, conflict refusal, already-completed startup, and cancellation after
Raw sync but before Catalog submission. The correction's local gates are:

- focused Gap/backpressure tests: `32 passed`;
- USD-M/Spot/Supervisor/Recovery/Archive expansion: `147 passed`;
- 1000 reconnect/rotation plus 100 overflow cycles: `2 passed`;
- million-event Raw stress: `1 passed in 195.94s`;
- default pytest: `594 passed, 28 skipped, 3 deselected`;
- Ruff, strict MyPy (193 source files), M0 contracts, Go Raw golden, and
  `git diff --check`: `PASS`.

The five-second parameter and stable error are now named for post-close queue
handoff. They do not claim an end-to-end Raw persistence or seal deadline;
drain, fsync, compression, seal, and Catalog work still depend on safely
finishing underlying I/O, and permanently hung kernel I/O remains a known risk.
The Draft PR remains undeployed and must obtain fresh macOS and Ubuntu final-head
CI before another ChatGPT Code Review handoff.

## Evidence and production boundary

The incident analysis is
`docs/milestone_evidence/M21.4-ingress-overflow-analysis.md`. Its home and
external compressed copies both have SHA-256
`59c1ba7b397dbdf54bf84d6ab15b9f9b34f03c74922687a98fefe7a31287452e` and
passed byte-for-byte comparison.

The unchanged production Wheel repeated the overflow during offline
development: PID 266907 became PID 271470 and `NRestarts` became 2. The second
incident was saved read-only; no deployment or service action followed.

No production service was stopped/restarted by this task; no production
configuration, unit, venv, Wheel, Catalog, Raw, proxy, mount, or registered
Archive object changed. Current production stability is observation only and
is not evidence that the old overflow defect is fixed.

## Post-review validation order

Only after Code Review, merge, and a separately authorized deployment may the
long-run sequence restart:

1. 2-hour preflight;
2. 12-hour stable-PID observation;
3. 24-hour canary;
4. new 72-hour T0;
5. continuous extension to 168 hours.

This milestone does not claim Production Ready.
