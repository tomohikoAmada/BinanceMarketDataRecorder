# M22.8 Acceptance — Remote Linux integrated failure acceptance

## Result and authority

M22.8 is **IMPLEMENTED / REAL CROSS-MACHINE FAILURE VALIDATED / ACCEPTED**.
The accepted run is `m22.8-20260822T041913Z-23f1fcc7` at exact main
`f699b6dc0e3e9e2d193eb9fd25321c59526995cd`.

The topology was MacBook local archive operator -> system OpenSSH -> Internet
-> real `sshd` -> Germany Ubuntu VPS `vps-b5bfe3f8` -> an isolated disposable
M22.8 workspace. No operation targeted production Raw, Catalog, systemd,
configuration, receipt, remote ownership, deletion authority, or accepted
M22.7B evidence. The remote RUN_ID workspace and local evidence remain
retained for forensic traceability after project-lead acceptance.

## Scenario result

| Scenario | Result |
| --- | --- |
| M8-00 isolation / topology preflight | PASS |
| M8-01 complete happy path | PASS |
| M8-02 transfer/operator crash and retry | PASS |
| M8-03 local receiver storage failure | PASS |
| M8-04 receipt / authorization / wrong identity | PASS |
| M8-05 delete response-loss ambiguity | PASS |
| M8-06 receipt-boundary crash and recovery | PASS |
| M8-07 snapshot failure and snapshot-only retry | PASS |
| M8-08 remote source read failure | PASS |

`SCENARIOS_EXPECTED=9`, `SCENARIOS_PRESENT=9`, and `SCENARIOS_PASS=9`.

M8-05 proved that the exact remote delete side effect completed while the
original SSH response was intentionally lost; the Mac observed an ambiguous
failure / exit 255, and a fresh process plus fresh SSH session reconciled the
same receipt to `REMOTE_DELETED`. No new receipt, source reselection, second
delete target, or neighbor-source deletion occurred.

M8-07 proved that a committed archive/delete lifecycle survives a failed
Catalog snapshot publication. `REMOTE_DELETED` and the same receipt remained
authoritative, and a snapshot-only retry succeeded without source reselection,
new receipt, reauthorization, or re-delete.

M8-03 and M8-08 both failed closed with no receipt, authority, or deletion;
their fresh retries succeeded with verified hashes. No source substitution was
observed.

## Milestone-wide gates

All required gates are PASS:

```text
ISOLATION_PROVEN=PASS
REAL_SSH_CROSS_MACHINE=PASS
NORMAL_TRANSFER=PASS
LOCAL_DURABLE_VERIFICATION=PASS
DURABLE_RECEIPT=PASS
TRANSFER_INTERRUPTION_RECOVERY=PASS
PROCESS_CRASH_RECOVERY=PASS
RETRY_IDEMPOTENCY=PASS
WRONG_IDENTITY_REJECTION=PASS
DELETE_AUTHORIZATION=PASS
REMOTE_DELETE=PASS
DELETE_RESPONSE_LOSS_RECOVERY=PASS
CATALOG_DR_SNAPSHOT=PASS
SNAPSHOT_FAILURE_RETRY=PASS
LOCAL_STORAGE_FAILURE_FAIL_CLOSED=PASS
REMOTE_STORAGE_FAILURE_FAIL_CLOSED=PASS
NO_SOURCE_SUBSTITUTION=PASS
PRODUCTION_DOMAIN_UNTOUCHED=PASS
```

Production configuration, systemd unit, deployment identity, release identity,
service state, and production domain were unchanged throughout. The observed
production service state remained `inactive/dead; enabled`.

## Retained disposable state and findings

The final audit intentionally did not delete all test Raw. Safe Raw remained
with authority `ABSENT` for M8-02, M8-03, and M8-08. Completed
`REMOTE_DELETED` lifecycles existed for M8-01, M8-04, M8-06, and M8-07. M8-05
retained only its independent neighbor Raw after its target reached
`REMOTE_DELETED`.

There are two nonblocking evidence-packaging P2 findings:

1. M8-02 has no dedicated `scenario.json`.
2. M8-06 has no dedicated `scenario.json` and lacks fully self-contained
   main/remote-host metadata.

Lifecycle, source, receipt, process-boundary, fixed-RUN_ID, and final
cross-scenario audit evidence exists. These findings do not invalidate the
semantics, require no scenario rerun, do not block M22.8 acceptance, and are
not M22.9 prerequisites. `OPEN_P0=0`, `OPEN_P1=0`, `OPEN_P2=2`.

## M22.9 boundary

M22.9 is the separate exact-VPS staged production acceptance. A historical
candidate deployment was attempted after this M22.8 record, but its 24h stage
is **INCOMPLETE**, it is not eligible for 72h, and the service is currently
STOPPED / NOT CAPTURING. The local startup-liveness correction is not yet
reviewed, merged, built, deployed, or accepted. The current record is
`docs/CURRENT_PRODUCTION_STATE.md`.

```text
exact artifact identity -> readiness -> 2h -> 12h -> 24h -> 72h -> 168h
```

`M22_9_RUNTIME_CREDIT=0` and `PRODUCTION_READY=NO`. No M22.8 evidence is
production acceptance or M22.9 duration evidence.
