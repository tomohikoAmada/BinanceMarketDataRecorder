# M22.9 Acceptance Evidence V3

## Status

This is the active engineering record for the acceptance-semantics correction.
It is implemented and offline validated on branch
`fix/m22-9-acceptance-evidence-v3`, from the frozen source authority
`5302de995729fe064044c41acf9224beeca4cfc4` / tree
`c68238dc9c7a12d1e85df15eda836b6dedd0ad0f`.

```text
MILESTONE=M22_9_ACCEPTANCE_EVIDENCE_V3
MILESTONE_STATUS=IMPLEMENTED_OFFLINE_VALIDATED_PENDING_INDEPENDENT_REVIEW
VPS_TOUCHED=NO
DEPLOYED=NO
FORMAL_V3_STARTED=NO
PRODUCTION_READY=NO
HISTORICAL_V2_EVIDENCE_MODIFIED=NO
```

The independently reviewed P1 acceptance-policy defect remains authoritative;
this record does not reopen that finding. The observed valid reconnect interval
was 1.909730489 seconds.

## Semantic contract

V3 records a structurally valid Catalog OPEN in the existing compact
`catalog_transition.current_open` projection. An intermediate OPEN is causal
state, not a blocker and not a sticky finding. A valid OPEN followed by its
exact COMPLETED lifecycle key is eligible once the reconstructed terminal
state is closed. An OPEN remaining at the terminal observation derives
`unresolved_discontinuity`, makes the final `FAIL`, and is ineligible.

The exact lifecycle identity remains
`(market, symbol, stream, gap_id)`. OPEN cannot disappear without an exact
valid completion. No grace timeout or new Catalog/Raw/manifest field was
introduced.

## Schema and compatibility

```text
SCHEMA_VERSION=m22.9-acceptance-evidence.v3
PREVIOUS_SCHEMA_VERSION=m22.9-acceptance-evidence.v2
LEGACY_SCHEMA_VERSION=m22.9-acceptance-evidence.v1
```

New identity, readiness, stage-start, stage-sample, and stage-final documents
are V3. New chains require V3 identity → readiness → 2h → 12h → 24h → 72h →
168h. Mixed V2/V3 predecessors are rejected explicitly.

V1 and V2 readers/verifiers retain their historical semantics. Historical V2
intermediate OPEN remains sticky `unresolved_discontinuity`; the historical
accepted V2 2h remains valid only for its old artifact, and the historical V2
failed 12h remains `INELIGIBLE` with zero credit. No historical file is
rewritten, upgraded, or deleted.

## Bounded verifier and resume behavior

Runtime `finalize()` performs exactly one terminal `sample()`, derives the
terminal blocker from the observer's bounded rolling Catalog OPEN state, and
does not rescan sample files or perform a second Catalog query. The V3 verifier
streams one sample at a time, reconstructs the terminal Catalog state, derives
the deterministic first-open detail from the sorted lifecycle key, and checks
the final blocker/detail/result/eligibility binding.

Resume restores T0, process/service/boot/deployment identity, hash-chain
position, rolling manifest/raw/reconnect state, and `published_catalog_open`.
Valid OPEN may cross observer interruption and later close; terminal OPEN and
silent disappearance remain fail-closed.

The existing one-time baseline, compact transitions, 300-second sample-start
cadence, BOOTTIME authority, and 600-second maximum evidence gap are unchanged.

## Offline validation

The implementation includes deterministic coverage for the real 1.909730489
second reconnect, sampling-phase invariance, terminal OPEN, resume OPEN→CLOSED,
resume terminal OPEN, silent disappearance, wrong/malformed/conflicting
completion, terminal-detail tampering, mixed schemas, historical V2 sticky
behavior, historical V1 readability, bounded chain state, and existing cadence
behavior.

The completed offline checks are:

```text
pytest -q                         1733 passed, 24 skipped, 4 deselected
ruff check .                      passed
mypy                              passed
tests/verify_m0_contracts.py      passed
python -m build                   passed (sdist and wheel)
git diff --check                  passed
```

The `python3.12 -m build` spelling was unavailable in the local interpreter
because that interpreter does not expose `build.__main__`; the available
repository Python executed the same build successfully.

## Deployment consequence

This source change requires independent review, a new wheel, a new deployment
identity, a stopped exact redeploy, fresh readiness, and a completely fresh V3
Formal chain. No credit transfers from the old V2 2h, and no 12h/24h/72h/168h
stage may start from a mixed or historical predecessor.

Recorder reconnect behavior, Catalog operational schema, Raw v1, manifest,
Archive, StorageLayout, Contracts, Projection, Gateway, transport policy,
systemd production service, capacity policy, VPS state, and Formal evidence
remain out of scope.
