# M22.7B Acceptance — VPS systemd, readiness, and exact deployment identity

## Result and authority

M22.7B implementation entered `main` through PR #25
`2a59a963a1fc622ccb58c6beb5c7d34cecc561f0` (tree
`f68ff9d7bc51eed964888b35060540f341fa43d2`). The earlier
`feat/m22-7b-vps-deployment-readiness` branch and base
`a21706c861654b107839dee6b0f1b41759c24c12` (tree
`796f7305d0cc62b742f1b20c7d20ee3062681da0`) are HISTORICAL PR #25
provenance only. M22.7A historical acceptance is unchanged. M22.7B is now
**IMPLEMENTED / REAL-HOST VALIDATED / ACCEPTED**. M22.8 remains
**NOT STARTED**, with its dependency gate satisfied.

This record accepts implementation, offline/static gates, and the corrected
exact-main real-host gate. An earlier host attempt on the implementation
artifact failed before first service start because `systemctl show` could not
prove the empty `EnvironmentFiles=` property. PR #26 corrected that proof
through direct systemd D-Bus evidence; the historical failure remains valid
provenance. The corrected exact artifact below was subsequently validated on
real Ubuntu 24.04.4 x86_64 / systemd 255.4. The service was intentionally
stopped after validation; no production acceptance window or production-ready
claim follows from this milestone.

## Real-host validation record

- Source: `650bc8f81446af5255d1eee8cfb6ab8b2ade5ccb`
- Host: Ubuntu 24.04.4 LTS, x86_64, Python 3.12.3
- systemd / busctl: `255.4-1ubuntu8.17` / `255.4-1ubuntu8.17`
- Wheel SHA-256: `ba6811097dbe008fd0c4c6a2aded47f48d192f1b942aa5dd11606df2deec9179`
- Dependency lock SHA-256: `44cd373324f2af5f2682851996bc59a16199c65f8de9e98089131e1c67d6f335`
- Deployment identity SHA-256: `26ab124a0d894596a30461eeedd651def0df42cf0503da5ef7db28e94c02f5f5`

The real systemd proof reported `EnvironmentFiles` D-Bus signature `a(sb)`
with count `0` and actual value `a(sb) 0`. The host target, exact source and
Wheel identities, dependency and filesystem ownership, unit and effective
systemd properties, empty environment authority, direct-network environment,
deployment identity, pre-start capacity, real service start, MainPID binding,
process environment, journald, graceful stop, restart, and rollback
compatibility preflight all passed. Readiness was `READY`.

The recorded validation result is:

```text
HOST_TARGET_MATCH=PASS
EXACT_SOURCE_IDENTITY=PASS
EXACT_WHEEL_IDENTITY=PASS
DEPENDENCY_IDENTITY=PASS
FILESYSTEM_OWNERSHIP=PASS
SYSTEMD_UNIT_IDENTITY=PASS
SYSTEMD_EFFECTIVE_PROPERTIES=PASS
SYSTEMD_ENVIRONMENTFILES_DBUS=PASS
DIRECT_NETWORK_ENVIRONMENT=PASS
DEPLOYMENT_IDENTITY=PASS
PRESTART_CAPACITY=PASS
REAL_SERVICE_START=PASS
MAINPID_BINDING=PASS
PROCESS_ENVIRONMENT=PASS
JOURNALD=PASS
READINESS=READY
GRACEFUL_STOP=PASS
RESTART=PASS
ROLLBACK_COMPATIBILITY_PREFLIGHT=PASS
```

One non-core USD-M side-data rate-limit warning occurred; core readiness
remained `READY`. The generic `doctor` command emitted a legacy x86_64 warning.
These are informational observations, not M22.7B blockers. The explicit VPS
profile and host gates passed.

`ROLLBACK_COMPATIBILITY_PREFLIGHT=PASS`. No actual artifact rollback was
exercised or required for this host gate:
`ACTUAL_ARTIFACT_ROLLBACK=NOT EXERCISED / NOT REQUIRED FOR THIS HOST GATE`.

## Explicit profile and dependency identity

`RecorderConfig.capacity_profile` accepts only omitted/`None` or the literal
`vps-production-v1`. Only `[recorder].capacity_profile` in the explicitly
loaded TOML selects the VPS profile. Unknown literals fail validation. Host,
platform, architecture, systemd, path, capacity, proxy, environment, and all
other config fields have no selection authority; there is no
`--capacity-profile` or profile environment variable.

Generic/local configuration retains existing Recorder environment behavior.
When the TOML explicitly selects the VPS profile, all
`BINANCE_MARKET_RECORDER_*` settings are rejected and proxy mode must be
`direct`. The certified unit contains no operational Recorder variable,
environment file, or pass-through environment authority; it explicitly empties
all upper- and lowercase standard proxy variables. Effective unit verification
and live process-environment verification independently reject routing drift.

`requirements/linux-x86_64-python312.lock` was generated with pip-tools 7.5.1
under Linux amd64/Python 3.12.13 using `--generate-hashes --strip-extras`, and
has SHA-256 `44cd373324f2af5f2682851996bc59a16199c65f8de9e98089131e1c67d6f335`.
The CI Ubuntu 24.04 job installs it with `--require-hashes` before the existing
CI/dev lock. A separate clean Ubuntu production venv installs only the exact
runtime lock, the Recorder Wheel, and the narrow `pip` bootstrap exception;
`pip check` and the installed-distribution identity verifier both run there.

The verifier parses the retained hash lock into normalized Python packaging
names and exact versions, then compares that complete set with actual installed
metadata. It rejects missing, wrong-version, or unexpected third-party
distributions and editable runtime installs. Recorder is deliberately excluded
from the dependency set and verified through the separate exact Wheel chain.

## Exact deployment and systemd authority

Canonical `deployment-identity.v1` binds the full source SHA, retained exact
Wheel and SHA-256, distribution version, exact Python executable and version,
retained dependency lock and SHA-256, config and SHA-256, installed unit and
SHA-256, capacity profile, effective systemd identity, current M22 remote-state
compatibility, and the consumed legacy classification authority as either an
exact present hash or explicit absent state. Parsing and serialization are
strict and deterministic.

Installed Recorder verification requires the exact retained local Wheel provenance from
`direct_url.json`, a non-editable install, matching Wheel hash, package version,
Python, module/dist-info location, and every installed SHA-256/size-bearing
`RECORD` entry. The interpreter must belong to the fixed production venv. The
root-owned artifact/release/venv/package control chain is checked component by
component without following controlling symlinks; service-writable directories,
files, or replacement points fail closed. It does not trust Git `HEAD`,
`--version`, or a rebuilt Wheel.

The VPS renderer fixes the system unit/config/data/Python roots and emits a
non-root `User=`/`Group=` service with only `network-online.target`, no Mihomo,
`Restart=on-failure`, `RestartSec=10s`, `TimeoutStopSec=90s`, `UMask=0027`,
`NoNewPrivileges=true`, and journald output. Install validation enforces the
root-controlled artifact/release/venv/config/unit and service-owned mode-0750
data boundary. Effective validation uses `systemctl show` to require the
expected fragment, empty drop-ins, exact semantic ExecStart argv, principal,
restart delay, stop timeout, UMask, NoNewPrivileges, working directory, service
type/signal/journal settings, effective Wants/Requires/After, and closed
environment authority. `network-online.target` must be wanted and ordered
after; `mihomo.service` is rejected from effective Wants, Requires, or After,
including `.wants`/`.requires` additions outside the fragment.

## Recovery, capacity, and lifecycle

The VPS runtime refuses to start without a validated runtime identity. It
publishes STARTING evidence, opens the Catalog, completes existing recovery,
takes an immediate actual internal-filesystem observation, and only then may
construct collectors. A periodic observation continues while running.

Actual free bytes <=10 GiB at startup prevents Collector construction. At
runtime it requests the ordinary graceful Collector stop and awaits completion,
which retains each Collector's drain/seal contract. Both paths record one
`DISK_EMERGENCY_STOP`, explicitly state that no unarchived Raw was deleted, and
open durable `session_restart` discontinuity evidence for every Spot/USD-M core
stream not already carrying an open gap. The termination reason is
`HARD_RESERVE_SAFETY_STOP`; normal return makes process exit zero, so the frozen
`Restart=on-failure` policy does not loop. A core runtime failure still raises,
publishes FAILED evidence, and exits nonzero/restart-eligible. ETA-derived
EMERGENCY above the actual reserve never stops collectors.

A later observation sees space consumed or released by co-resident processes
without cause attribution. Release after a clean hard-reserve stop never starts
the service; the operator must explicitly start it and rerun all startup gates.

## Readiness and rollback

`deployment readiness` has an external 300-second deadline and no HTTP server
or runtime watchdog. READY requires the expected active unit, MainPID equal to
the live fresh service-state PID, exact runtime/file/effective identity, selected
profile, open read-only-valid Catalog, completed recovery, existing full Spot
and USD-M core readiness, and a current valid profile capacity observation with
actual free bytes above 10 GiB. WARNING/CRITICAL/EMERGENCY above 10 GiB and an
insufficient ETA history with a current safe observation may remain READY.
Startup and intentional stopped states are NOT_READY; invalid/missing identity,
failed recovery/service, stale/dead claimed running state, invalid/unavailable
capacity, or deadline expiry are FAILED.

Stopped deploy/upgrade procedures preserve exact old and new bundles and never
mutate a running venv. Rollback compatibility reads current Catalog remote
transactions and fails closed unless the exact preserved M22 target declares
every durable state. A pre-M22 identity cannot pass. Rollback never changes
Raw, Catalog, receipts, lifecycle state, or data. No new remote-delete authority
was added.

## Deterministic validation

Focused profile, identity, systemd, readiness, runtime, CLI, shared-host, and
rollback tests pass. They cover explicit/default/invalid/no-implicit profile;
all exact file mismatches; normalized dependency version/missing/extra drift;
protected/writable/symlinked venv and release chains; canonical identity;
sidecar present/absent; drop-ins, exact effective dependencies/properties/argv,
environment-file/pass-through, upper/lower proxy, and live process environment
rejection; active-alone, PID, stale, identity,
recovery, both markets, capacity, reserve, degraded-safe, insufficient-history,
and a full READY path through the real file-identity and effective-systemd
verification seams; startup/runtime hard reserve; actual private CLI zero exit
for clean safety stop versus nonzero unexpected failure;
shared-host consume/release without attribution; and compatible/incompatible or
pre-M22 rollback.

Final pre-commit local results on macOS Apple Silicon/Python 3.12:

- focused M22.7B matrix: **179 passed**;
- full offline `python -m pytest -q`: **1264 passed, 24 skipped, 4
  deselected**, with 13 existing multiprocessing fork warnings;
- `python -m ruff check .`: passed;
- strict `python -m mypy`: passed across 236 source files;
- `python tests/verify_m0_contracts.py`: passed;
- `go run tools/verify_raw_chunk_golden.go`: passed;
- `python -m build --no-isolation`: built sdist and Wheel;
- clean-Wheel install and version/config/doctor/status CLI smoke: passed;
- clean-Wheel installed identity audit: non-editable exact `direct_url.json`,
  module/dist-info consistency, and 115 `RECORD` file hashes/sizes passed;
- a local Docker validation container using Linux amd64/Python 3.12.13 (test
  tooling only, not certified deployment) created a clean copied venv, installed
  the production lock with `--require-hashes`, installed the Wheel separately,
  passed `pip check`, and proved the exact 28/28 installed locked distribution
  set with only the explicit `pip` bootstrap exception;
- `git diff --check`: passed.

The following were not run and are not claimed: actual artifact rollback,
stopped host upgrade, online Binance smoke, stress, physical archive media,
M22.8, or any 2h/12h/24h/72h/168h window. The real root-owned filesystem and
system unit installation, daemon-reload/enable/start, non-root MainPID and
journald evidence, SIGTERM/crash/systemd restart behavior, and disposable
low-space filesystem behavior are covered by the accepted real-host record
above.

## Compatibility boundary

Raw/EventEnvelope bytes and schemas, Catalog schema/state transitions, archive
receipts, RemoteTransport, SSH, remote authorization/deletion, Catalog snapshot
authority, generic M11 capacity behavior, and M22.7A numeric policy are
unchanged. Capacity pressure never authorizes local or remote Raw deletion.
M22.7B acceptance is the deployment/readiness host gate only. It creates and
validates the exact deployable/readiness substrate but does not claim
Production Ready. The final exact M22 artifact must still pass, after M22.8,
the independent `exact identity -> readiness -> 2h -> 12h -> 24h -> 72h ->
168h` chain.
