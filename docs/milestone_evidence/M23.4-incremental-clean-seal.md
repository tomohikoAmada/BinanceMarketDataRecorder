# M23.4 Incremental Clean-Seal Evidence

Status: **COMPLETE / MERGED / INDEPENDENT CORRECTNESS APPROVED / POST-MERGE
CI PASS / 2H PRODUCTION-EQUIVALENT PERFORMANCE PASS**

Base authority: `86e056cbcaebb6ec2a84c00e31e6854b7898aa7e`

Branch: `perf/m23-4-incremental-clean-seal`

Reviewed implementation head: `5d89e467eba50e2758b7187d937b3ca62dcbc808`

First correction head: `05a04a45ca7d8132e41e89ee200cf83617ced51a`

Final P1 correction head: `3ba1c217e5d1a68c4ee6d27439ad60a8d7ec93a0`

Merged through PR #41. Current main/merge authority:
`e074d41a979af92b50bee880d6d55295ca65413d`.

Post-merge `offline-ci` run `33174563501` completed successfully at that exact
head. Final independent correctness review recorded `P0=0`, `P1=0`, `P2=0`,
`P3=0` and `M23_4_FINAL_CORRECTNESS_APPROVED=YES`.

Independent exact-diff correctness review recorded `P0=0`, `P1=0`, `P2=2`,
and `P3=0`; the core M23.4 architecture was approved. The two P2 findings were
limited to Raw-v1 header wire-bound parity and retained active-source cleanup
after same-host archive reservation. Both are corrected narrowly here without
reopening the approved writer, evidence, seal, queue, or archive protocols.

Targeted rereview confirmed those two P2 findings closed. It found one new P1
introduced by the first correction: ordinary `SEALED` metadata plus matching
manifest/Catalog/retained-Raw identity could authorize unlink while the actual
sealed `.zst` was absent, deleting the last Raw copy. This correction requires
ordinary `SEALED` to pass full `validate_sealed_artifact()` before retained-Raw
unlink. A missing or corrupt artifact fails closed with the retained Raw still
present. It deliberately does not re-enter `seal_partial()` from a stale
`SEALED` observation, so ArchiveManager reservation cannot be reversed.

M23.0/M23.0F production-equivalent profiling separately authorized this
execution-order override before formal M22.9 acceptance. The observed primary
seal cost was the synchronous second Raw scan/hash, not compression/readback.
M23.1/M23.2 speculative work was skipped and M23.3 was not required before this
milestone. The implementation portion of this record is historical correctness
evidence. The post-merge section below separately records deployment and
non-formal VPS A/B evidence. Neither is formal M22.9 acceptance evidence.

## Trust boundary

For each append, `RawChunkWriter` first copies the caller's full Raw semantics
into one private immutable snapshot. `source_sequence` receives a private copy
behind a read-only mapping; flags are immutable. The exact canonical CBOR frame
and the statistics/connection-transition delta derive only from that snapshot.
After the complete exact frame write succeeds, and only then, the writer commits
the exact bytes to incremental SHA-256/verified byte count and commits semantic
statistics/transitions. Physical write progress is tracked separately.

Any partial/zero/exception write, evidence-commit failure, fsync failure, close
failure, observer/invariant failure after write, or closed-size mismatch poisons
the writer. A poisoned writer cannot issue clean evidence. A successfully
final-fsynced and closed writer can transfer its frozen memory-only evidence
exactly once through the writer-bound private clean-seal entry.

`seal_partial(path, ...)` has no evidence parameter and always calls
`scan_chunk(path)`. Recovery, crash leftovers, `RECOVERED`, poisoned, and
unknown partials retain full scan authority. The rare zero-record reconnect
marker also stays on `seal_partial()`.

Raw-v1 header eligibility now uses one `MAX_HEADER_BODY_BYTES = 64 * 1024`
authority. The encoder rejects a canonical header body above that exact bound
before any writer partial can be created; the decoder uses the same constant.
The bound and Raw-v1 wire layout are unchanged.

## Shared durable protocol and byte gate

Both scan-derived and live-writer-derived evidence enter one private shared
implementation for:

```text
ACTIVE / RECOVERED
  -> SEALING
  -> compress + fsync
  -> decompressed readback
  -> atomic sealed rename + directory fsync
  -> manifest
  -> SEALED
  -> source partial unlink + active-directory fsync
```

The compressor's actual consumed/stored counts are checked. Decompressed byte
count and SHA-256 must exactly match verified Raw evidence before rename. Stored
size/SHA, Zstd content size/checksum, atomic publication, manifest identity, and
Catalog ordering remain intact. A restart after Catalog `SEALED` but before
source unlink discovers the retained file only in `active/`. Recovery freshly
full-scans that retained Raw, then validates header/chunk identity,
uncompressed size/SHA-256, manifest, Catalog row, sealed artifact while its
same-host lifecycle still requires that source, and the exact archive
transaction/state. Only then does it unlink and fsync the redundant active
source. For ordinary `SEALED`, the sealed artifact is mandatory and receives
full stored and decompressed size/SHA-256 validation; missing or corrupt
authority preserves the retained Raw and fails startup recovery closed. If
ArchiveManager wins the post-`SEALED` race and advances through
`ARCHIVE_COPYING`, `ARCHIVE_VERIFYING`, `ARCHIVED_VERIFIED`,
`LOCAL_DELETE_PENDING`, or `LOCAL_DELETED`, cleanup preserves the winning
state and transaction and performs no Catalog transition. A clean but
nonmatching retained Raw fails closed and remains present.

## Compatibility

- Raw format/schema changed: **NO**
- Catalog schema changed: **NO**
- Durable seal protocol changed: **NO**
- Recovery authority changed: **NO**; unknown memory provenance always scans
- Manifest gap/completeness semantics changed: **NO**
- Zero-record reconnect marker semantics changed: **NO**
- Queue capacity, scheduling, threading, or compression level changed: **NO**
- Archive lifecycle or transaction protocol changed: **NO**
- New durable cleanup authority or schema added: **NO**
- Ordinary-`SEALED` retained-source deletion authority narrowed: **YES**;
  actual validated sealed Raw is now mandatory

## Validation matrix

The implementation includes deterministic tests for:

- exact incremental-versus-scan header/statistics/transition/size/SHA equality;
- 32 bounded seeded combinations covering zero/multiple records, payload sizes,
  optional exchange clocks, integer/string/mixed sequences, flags, connections,
  collector IDs, transitions, gaps, resync, overlap, and deployment IDs;
- controlled cross-thread caller mutation after snapshot but before write;
- `created_at_utc_ns` negative/zero/positive behavior, identity requirements,
  and both supported `max_frame_bytes` bounds;
- partial/zero/exception writes, complete physical write before evidence commit,
  evidence commit failure, periodic/final fsync, and close failure;
- header/frame-prefix/frame-body bit mutation, truncation, append-extra-bytes,
  incorrect compressor count, compression/readback/rename/manifest/Catalog
  failures, and Catalog `SEALED` before source unlink;
- clean-versus-scan manifest semantic equality;
- clean rotation scan spy, general `seal_partial()` scan spy, recovery seal scan
  spy, and zero-record marker scan spy;
- existing reconnect-boundary, M22.9, crash-recovery, and Raw golden suites.
- ordinary and oversized header encode/decode eligibility, including proof
  that an oversized header cannot create a clean writer;
- injected post-`SEALED` active-source unlink failure followed by deterministic
  Archive reservation and startup recovery at every reachable same-host
  successor state;
- reservation during retained-source validation, exact lifecycle/transaction
  preservation, repeated-startup idempotence, unchanged sealed/manifest/
  external bytes, and fail-closed rejection of a clean wrong Raw source.
- ordinary `SEALED` with a retained clean Raw and a missing, correct, or corrupt
  sealed artifact, including validation-before-unlink ordering and last-copy
  preservation.

### Executed gates

- Exact new-P1/validation/corruption regressions: **3 passed**.
- Retained-source/archive recovery and ArchiveManager lifecycle set:
  **35 passed**.
- Incremental clean-seal, Raw format, and crash/full-scan recovery set:
  **64 passed**.
- Reconnect integrity, legacy/orphan recovery, historical audit, Spot/USD-M
  ingress, and USD-M session recovery set: **226 passed, 1 deselected**.
- M22.9 startup/service/acceptance regression set: **112 passed**.
- Python Raw golden: **1 passed**; independent Go Raw-v1 golden verification:
  **passed**.

- `CI=true M0_CONTRACT_ROOT=<isolated-worktree> python3.12 -m pytest -q`:
  **1414 passed, 24 skipped, 4 deselected**. The skips are explicitly opt-in
  live Binance/developer-preview tests. The supported exact-root override is
  required because the frozen M0 verifier otherwise intentionally rejects any
  path other than the canonical checkout, while M23.4 explicitly requires an
  isolated worktree.
- `python3.12 -m pytest -m stress -q tests/stress/test_million_events.py`:
  **1 passed**.
- `python3.12 -m ruff check .`: **passed**.
- `python3.12 -m mypy`: **passed (244 source files)**.
- `CI=true M0_CONTRACT_ROOT=<isolated-worktree> python3.12
  tests/verify_m0_contracts.py`: **passed**.
- `go run tools/verify_raw_chunk_golden.go`: **passed**.
- `git diff --check`: **passed**.

The implementation commit is recorded in the final milestone handoff because a
commit cannot embed its own non-self-referential identity.

## Post-merge two-hour VPS evidence

The exact merge candidate was built and deployed on the Recorder VPS without
changing queue capacity, compression, worker count, collection scope, polling,
or systemd limits.

```text
SOURCE_SHA=e074d41a979af92b50bee880d6d55295ca65413d
WHEEL=binance_market_data_recorder-0.1.0a1-py3-none-any.whl
WHEEL_SHA256=7dfef238514dbb3fc1ceb56e1b395eefbb8a85516bd4ffcf784daaf1260634a1
LOCK_SHA256=44cd373324f2af5f2682851996bc59a16199c65f8de9e98089131e1c67d6f335
CONFIG_SHA256=5aee65a7de55cf06645c70296870346004c712fc6f9cd43390e1ea8b3ffabfbb
UNIT_SHA256=69f3c4c2c77a3e6fc4ee397d26ecb2927a741a6165024b2f6656727d0b398b83
DEPLOYMENT_IDENTITY=ed1b108c33cf31ad8faceddbd66814804e1411cdbc6250eef1526365c52dece2
```

The B run completed `7200.001773005002 s` continuously READY. It recorded zero
backpressure episodes/timeouts, restart, SIGKILL, OOM, resync, or terminal
failure. Normal live-owned clean-seal `scan_chunk` was **NOT OBSERVED**;
`scan_chunk` was `0.0%` of global samples and the seal path was
`1.5320266524015844%`.

| Metric | M23.0F A | M23.4 B |
| --- | ---: | ---: |
| Duration (s) | `7200` | `7200.001773005002` |
| CPU average (%) | `41.875703418339505` | `85.61719301067274` |
| CPU peak (%) | `92.98354726423364` | `103.89955576291902` |
| Backpressure episodes | `3` | `0` |
| Backpressure total (s) | `22.305506229400635` | `0` |
| Backpressure maximum (s) | `11.086111068725586` | `0` |
| Backpressure timeouts | `0` | `0` |
| Queue maximum | `8192` | `7325 / 8192` |
| Maximum seal latency (s) | `11.532059644` | `2.043359872` conservative |
| `scan_chunk` global (%) | `9.27279999179671` | `0.0` |
| `scan_chunk` share of seal (%) | `91.85290532303955` | `0.0` for normal clean seal |

B sealed 1,137 chunks. Seal latency was `0.005654295 s` minimum,
`0.116858659 s` p50, `0.835251814 s` p95, `1.191445053 s` p99, and
`2.043359872 s` conservative maximum. RSS moved from `219152384` to
`255397888` bytes with a `263421952` byte peak. Maximum seal latency fell
`82.28105008923417%`.

B sealed 9,464,735 records versus A's 3,013,877, a 3.140385291 ratio. Raw CPU
percentages therefore are not a like-for-like regression measure. The frozen
workload summary gives CPU seconds per million sealed records of
`1000.3891996919582` for A and `651.3061379954114` for B, an improvement of
`34.894725153374026%`.

The run recorded 17 external disconnect intervals: 17 STARTED, 17 COMPLETED,
and 0 OPEN. A later M22.9 precondition inspection classified them as
`CLOSED_HISTORICAL_BASELINE`. They are not 17 unresolved gaps, and the run did
not have zero network disconnects.

Two operator incidents are disclosed. `/dev/null` was briefly replaced during
an operator check and immediately restored. After the completed two-hour
measurement, an optional unbounded full-history audit used excessive RAM and
was terminated. The Recorder did not restart, receive SIGKILL, OOM, or produce
a terminal failure, and neither incident invalidated the completed window. Do
not repeat the unbounded audit on this small VPS.

Evidence is retained outside GitHub:

```text
/opt/binance-market-data-recorder/m23-profiling/M23.4-POST-MERGE-20260828T143255Z
/opt/binance-market-data-recorder/m23-profiling/M23.4-POST-MERGE-20260828T143255Z.tar.gz
BUNDLE_SHA256=cb3c2998090545778dd5a0dc2d1cec333d08729120fb8a097de053a63fddb1a2
```

Final non-formal verdict:

```text
M23_4_RUNTIME_INTEGRITY_PASS=YES
M23_4_STRUCTURAL_PERFORMANCE_PASS=YES
M23_4_PERFORMANCE_IMPROVED=YES
FINAL_VERDICT=M23_4_POST_MERGE_AB_PASS
```

## Remaining boundary

M23.4 itself is complete. Progressive non-formal 4h, approximately 12h, 24h,
and 72h burn-ins remain future engineering validation. Formal M22.9 has not
started, no M22.9 T0 exists for this candidate, and Production Ready is not
authorized.
