# M21.4.11 72h Failure and Reconnect Boundary Integrity Repair

## Status

**FORMAL_72H_RESULT=FAIL**. Core process stability PASS; data-integrity
contract FAIL. **Eligible for next stage from 72h evidence: false. 168h not
started. Not Production Ready. NOT DEPLOYED.**

The M21.4.9 report (`final-review-20260810T022504Z`) claimed
FORMAL_72H_RESULT=PASS. It was superseded by the M21.4.10 unexpected-disconnect
forensic review (`unexpected-disconnect-forensics-20260810T042356Z`,
UNEXPECTED_DISCONNECT_CONTRACT_RESULT=FAIL), which this document records
together with the M21.4.11 forward-fix design and the historical read-only
audit results.

## 1. Formal 72-hour window identity [OBSERVED]

| Item | Value |
|---|---|
| RUN_ROOT | `/var/tmp/bmdr-m21-4-72h-20260807T021026Z` |
| T0 | 2026-08-07T02:10:34.950152Z |
| Target | 2026-08-10T02:10:34.950152Z |
| production_code_commit | `cf1e749c7a533e916dbfb685212e5549a38c70dd` |
| documentation_revision | `a16222775894fe879eb2d6d3f926b1c0f43f7253` |
| Wheel SHA-256 | `926615b09ef46130f49a87fe8ab20acb7cfa6313daa67af5b718931bd95ff329` |
| MainPID | 317289 unchanged; NRestarts=0; boot_id unchanged |
| CORE_PROCESS_STABILITY_RESULT | **PASS** |
| DATA_INTEGRITY_CONTRACT_RESULT | **FAIL** |
| FORMAL_72H_RESULT | **FAIL** |
| ELIGIBLE_FOR_NEXT_STAGE_FROM_72H_EVIDENCE | **false** |

## 2. The 14:08 unexpected disconnect [OBSERVED]

2026-08-07T14:08:24.383388Z USD-M `book_ticker` ConnectionClosedError
(`usdm_websocket_disconnected`, retry=1):

- old connection `16541a79-1144-419f-a5e4-427d3d485ee8` (gen 9)
- new connection `9aab9a06-db4a-4ecf-ac76-c00b3e67ab2f` (gen 9)
- **SAME_GENERATION=true**; same writer task; same chunk `7917819a`
- receive gap ~1.7266s; first-new frame index 5576 with `capture_flags=[]`
- Catalog: no `STREAM_DISCONTINUITY_STARTED`/`COMPLETED`
- Raw: `sequence_gap=false` on both boundary frames
- Manifest `7917819a`: `gap=false`, `complete=true`

**UNEXPECTED_DISCONNECT_CONTRACT_RESULT=FAIL.** Exchange-side completeness
between the close and the first new frame cannot be proven; the Recorder
cannot prove the exchange sent no events in the interval (u jumped by
56,294,564; same-connection max delta was 16,773). `book_ticker` has no
sequence continuity proof and no snapshot recovery, so the interval must
remain `gap=true`/`complete=false`/`historical_continuity_restored=false`.

## 3. Root cause (confirmed independently from source) [DERIVED]

`src/binance_market_data_recorder/binance/usdm/websocket.py` (cf1e749):

- `_receive_connection()` returns `"planned_rotation"` on the rotation
  timeout and re-raises `(WebSocketException, OSError, TimeoutError)` on a
  disconnect.
- `_connection_loop()` catches the disconnect, logs it, backs off, and
  `continue`s with a **new `connection_id` but the same writer task and the
  same `_generation`**.
- Only `IngressBackpressureTimeout` produces gap evidence: boundary frame +
  `_record_gap_started` + generation++ + recovery marker.
- `seal_partial()` derives `complete` solely from Raw capture flags, so a
  cross-connection chunk with no flags seals as `gap=false/complete=true`.

The same structural gap exists in `binance/spot/websocket.py`
(`planned_rotation`, `server_shutdown`, `unexpected_disconnect` all continue
in one writer/generation) and in the depth-driven capture-session restarts:
`book_ticker`/`agg_trade`/`mark_price`/`liquidation` connections closed by a
session restart reopen in the next session with no boundary evidence.

## 4. Reconnect Path Matrix [DERIVED]

| Path | exception/event | old conn closes | new conn opens | same writer? | same generation? | old generation sealed? | Catalog STARTED? | first-new sequence_gap? | Catalog COMPLETED? | old manifest gap/complete | new manifest gap/complete | diff_depth resync? | contract result (before fix) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ingress_backpressure | `IngressBackpressureTimeout` (close 1013) | yes | yes | no (new writer per generation) | no (gen++) | yes | yes | yes (boundary frame + first-new) | yes | gap=true/complete=false | gap=true/complete=false | yes (session retire) | PASS |
| unexpected_disconnect | `WebSocketException`/`OSError`/`TimeoutError` from recv | yes | yes (backoff) | **yes** | **yes** | no | **no** | **no** | **no** | **gap=false/complete=true** | same chunk | yes for diff_depth (lifecycle observer) | **FAIL** |
| planned_rotation | 23h50m timeout close(1000) | yes | yes | **yes** | **yes** | no | **no** | **no** | **no** | **gap=false/complete=true** | same chunk | yes for diff_depth | **FAIL** |
| server_shutdown (Spot) | serverShutdown frame | yes | yes | **yes** | **yes** | no | **no** | **no** | **no** | **gap=false/complete=true** | same chunk | yes for diff_depth | **FAIL** |
| transport failure before first frame (opener) | connect error | n/a (no data) | retry same gen | yes | yes | n/a | no | n/a | n/a | n/a | n/a | n/a | OK (no data at risk) |
| session restart (depth resync trigger) | `session_stop` closes all streams | yes | yes (new session) | no (new writer) | no (new session) | yes (plain seal) | **no** | **no** | **no** | **gap=false/complete=true** | per stream | yes | **FAIL** |
| graceful shutdown | `stop` event | yes | **no** | yes | yes | yes (plain seal) | no | no | no | complete (correct) | n/a | n/a | PASS (no false gap) |
| writer failure | writer task exits | n/a | no (fatal) | n/a | n/a | abort/partial | no | n/a | n/a | recoverable partial | n/a | n/a | PASS (fail closed) |

## 5. Planned rotation is not an integrity exemption [OBSERVED]

Local-host read-only forensics (production Catalog/Raw/Manifest, never
modified) via `tools/audit_reconnect_boundaries.py`:

| Rotation | UTC window | stream evidence | catalog gap | manifests |
|---|---|---|---|---|
| 12h window rotation (2026-08-05T06:52-06:53Z) | all 8 WebSocket streams show a connection transition | none (`UNMATCHED`) | old/new `gap=false`, `complete=true` |
| 24h window rotation (2026-08-06T06:43Z) | all 8 WebSocket streams | none (`UNMATCHED`) | `gap=false`, `complete=true` |
| 72h rotation 1 (2026-08-07T06:33Z) | all 8 WebSocket streams | none (`UNMATCHED`) | `gap=false`, `complete=true` |
| 72h rotation 2 (2026-08-08T06:23Z) | all 8 WebSocket streams | none (`UNMATCHED`) | `gap=false`, `complete=true` |
| 72h rotation 3 (2026-08-09T06:14Z) | all 8 WebSocket streams | none (`UNMATCHED`) | `gap=false`, `complete=true` |
| post-72h rotation (2026-08-10T06:02-06:05Z) | 8 further unmarked transitions | none (`UNMATCHED`) | `gap=false`, `complete=true` |

**PLANNED_ROTATION_RAW_CONTRACT_RESULT=FAIL.**

Consequences:

- `REPEATED_ROTATION_BEHAVIOR=PASS` (recorded in the 12h/24h evidence) may
  only continue to mean **PROCESS/ORDERBOOK_ROTATION_STABILITY=PASS**. It no
  longer supports **RAW_DATA_INTEGRITY=PASS**.
- **FORMAL_12H_DATA_INTEGRITY_STATUS=FAIL** (reopened). The 12h window's
  process-stability result is retained; its data-integrity acceptance is
  superseded.
- **FORMAL_24H_DATA_INTEGRITY_STATUS=FAIL** (reopened). The 24h window's
  process-stability result and the gen5 backpressure
  RECOVERY_CONTRACT_PASS are retained; the window's data-integrity
  acceptance is superseded.
- The 2h preflight result is retained only as evidence for the old artifact's
  short-window process stability; it validates nothing about reconnect
  boundary integrity.

Historical reports are not deleted; the affected claims are marked
`SUPERSEDED_BY_RECONNECT_INTEGRITY_FINDING` in the acceptance and evidence
documents.

## 6. Historical impact [OBSERVED]

### 6a. Original scanner (M21.4.11 initial head) [SUPERSEDED]

Read-only audit of all 160,794 sealed chunks in the production root
(`/var/lib/binance-market-data-recorder`) on 2026-08-10 (final run after the
2026-08-10 planned rotation), classified at the manifest level:

| Stream | chunks | transitions | explicit gap | blue/green overlap | unmarked reconnect |
|---|---|---|---|---|---|
| spot/agg_trade | 18,010 | 746 | 0 | 0 | 746 |
| spot/book_ticker | 18,013 | 792 | 0 | 0 | 792 |
| spot/diff_depth | 18,013 | 792 | 0 | 0 | 792 |
| um_perpetual/agg_trade | 17,753 | 561 | 0 | 0 | 561 |
| um_perpetual/book_ticker | 17,754 | 583 | 11 | 0 | 572 |
| um_perpetual/diff_depth | 17,753 | 566 | 0 | 0 | 566 |
| um_perpetual/liquidation | 3,064 | 129 | 0 | 0 | 129 |
| um_perpetual/mark_price | 17,652 | 522 | 0 | 0 | 522 |
| **Total (WebSocket streams)** | **160,794** | **4,691** | **11** | **0** | **4,680** |

These figures were produced by a scanner that classified whole adjacent
manifests instead of the exact connection boundary (M21.4.11-R2), embedded
volatile wall-clock values in the claimed-deterministic payload (R5), and
called `ensure_storage_layout` on the audited tree (R3). They are retained
only as
`SUPERSEDED_BY_CORRECTED_BOUNDARY_LOCAL_AUDIT`; see section 6b.

- The 11 explicit transitions are exactly the known USD-M `book_ticker`
  backpressure recovery cycles (gen0-gen11), which already carried full gap
  evidence.
- The 7 known `book_ticker` chunks with multiple connection_ids
  (`174a8c8d`, `175b9760`, `3c8515dd`, `4f1ae423`, `6edf40f7`, `7917819a`,
  `c69336ea`) are all identified: `gap=false`, `complete=true`, no flags,
  no Catalog gap. `7917819a` is the 14:08 chunk. Their local sealed copies
  are already deleted after verified archival, so the audit classifies them
  as `intra_chunk_unordered` manifest-level evidence
  (`frame_detail_unavailable=true`).
- `agg_trade`, `diff_depth`, `mark_price`, `liquidation`, and all five
  planned rotations show the same silent-gap class; the defect is systemic,
  not a single-event anomaly.
- The audit also confirms that REST-polled streams (depth_snapshot,
  exchange_info, 5m statistics) mint one connection_id per request; their
  connection transitions are not reconnect boundaries and are excluded from
  the analysis (documented in the tool).
- Production keeps producing unmarked boundaries on the unchanged artifact:
  8 transitions from the 2026-08-10T06:04Z planned rotation occurred after
  the 72h Target.

Audit artifact: `RUN_ROOT/historical-reconnect-audit.json`
(SHA-256 `92943961d561a44040007c6feb5ee01839af49a7c68a0178c70709bab1feedb4`).
The tool is read-only; no Raw, manifest, Catalog, or index was modified.

### 6b. Corrected boundary-local scanner (M21.4.11-R1..R5) [AUTHORITATIVE]

The corrected scanner classifies every transition boundary-locally (exact
boundary frame pair, boundary-specific manifest proof, exact Catalog
connection-pair identity), is strictly read-only, and emits a deterministic
canonical payload with an explicit cutoff and immutable manifest inventory.

Corrected read-only rerun on 2026-08-10 (production root still running the
unchanged artifact):

- `audit_cutoff_utc_ns`: `1786349202047196027`
- `manifest_inventory_count`: `161,817`
- `manifest_inventory_sha256`:
  `ffaf34bdc29c016b0251f64252bc2c35edd43faba014c030b0834b9cc585dad3`
- canonical payload SHA-256:
  `7143bc0cd3370c831df773a5bd9246d86ee95377beb57e08097519a9c8a520b3`
  (byte-identical across two runs with the same cutoff)

| Stream | chunks | transitions | explicit gap | blue/green overlap | unmarked reconnect | unknown/ambiguous |
|---|---|---|---|---|---|---|
| spot/agg_trade | 18,126 | 746 | 0 | 0 | 746 | 0 |
| spot/book_ticker | 18,129 | 792 | 0 | 0 | 792 | 0 |
| spot/diff_depth | 18,128 | 792 | 0 | 0 | 792 | 0 |
| um_perpetual/agg_trade | 17,868 | 561 | 0 | 0 | 561 | 0 |
| um_perpetual/book_ticker | 17,870 | 583 | 11 | 0 | 572 | 0 |
| um_perpetual/diff_depth | 17,868 | 566 | 0 | 0 | 566 | 0 |
| um_perpetual/liquidation | 3,080 | 129 | 0 | 0 | 129 | 0 |
| um_perpetual/mark_price | 17,767 | 522 | 0 | 0 | 522 | 0 |
| **Total (WebSocket streams)** | **161,817** | **4,691** | **11** | **0** | **4,680** | **0** |

- The corrected totals confirm the superseded figures (4,691 / 11 / 4,680)
  with boundary-local evidence: every one of the 11 explicit transitions is
  proven by exact Catalog connection-pair identity
  (`catalog_identity_match=true`), and no transition is classified explicit
  from an unrelated adjacent-manifest flag.
- The 4,680 unmarked transitions carry no gap evidence on either adjacent
  manifest; they remain unreliable.
- The 11 Catalog `STREAM_DISCONTINUITY_STARTED` events pair exactly with 11
  `COMPLETED` events (`matched_pairs=11`, `unmatched_started=0`,
  `unmatched_completed=0`).
- Read-only proof: the before/after `find -xdev` inventory diff across the
  audit run contains only the running recorder's own writes (new partials,
  sealed artifacts, manifests, Catalog WAL/heartbeat); no tool-created file
  or directory exists under the production root.

Audit artifact:
`/var/tmp/m21-4-11-corrected-historical-audit.json` (canonical payload
SHA-256 `7143bc0cd3370c831df773a5bd9246d86ee95377beb57e08097519a9c8a520b3`).

## 7. Forward fix: unified Reconnect Boundary state machine [DERIVED]

Implemented in this PR (`fix/m21-4-reconnect-boundary-integrity`). Both Spot
and USD-M collectors route every transport boundary through one state
machine; the M21.4.11-R1 correction makes the intent crash-durable by
persisting Catalog STARTED **before** the old-generation seal:

```
CONNECTED(generation N)
  -> boundary detected (ingress_backpressure / unexpected_disconnect /
     planned_rotation / server_shutdown / session_restart)
  -> CATALOG_GAP_STARTED_DURABLE (STREAM_DISCONTINUITY_STARTED)
  -> DRAIN_OLD_GENERATION
  -> SEAL_OLD_GENERATION_INCOMPLETE
  -> generation N + 1
  -> OPEN_NEW_CONNECTION
  -> FIRST_NEW_FRAME(sequence_gap)
  -> RAW_SYNC (StreamSpool.sync)
  -> CATALOG_GAP_COMPLETED (STREAM_DISCONTINUITY_COMPLETED)
  -> CONNECTED(generation N + 1, historical_continuity_restored=false)
```

Crash phases and their recovery behavior are documented in ADR-0027
(`BOUNDARY_INTENT_DURABLE` -> `OLD_GENERATION_SEALING` ->
`OLD_GENERATION_SEALED_INCOMPLETE` -> `NEW_GENERATION_AUTHORIZED` ->
`FIRST_NEW_RAW_SYNCED` -> `DISCONTINUITY_COMPLETED`); startup recovery
derives forced `reconnect_gap` seal flags from the durable open-gap state,
so a crash at any phase can never re-seal the old generation complete=true.

The intent decision has two points (M21.4.11-R1 independent review): the
normal STARTED-before-seal path, plus a post-drain re-check. When the
pending gap's first-new frame is still in the writer queue at boundary
detection, the pre-seal decision skips a new STARTED; the boundary drain
then persists that frame and records COMPLETED for the earlier gap, so the
current boundary would otherwise open its replacement with no durable
intent and an unmarked first frame. The collector therefore re-checks after
the drain and records STARTED for the current boundary before the
replacement connection opens (INV-007/INV-009/INV-010). Three deterministic
race tests (USD-M book_ticker, USD-M agg_trade, Spot book_ticker) hold the
writer drain until the boundary decision has run, then release it; each
fails without the post-drain intent record.

Rules enforced by the implementation:

1. **No unpersisted last-old frame is ever mutated.** For unexpected
   disconnect / planned rotation / server shutdown / session restart there is
   no in-hand boundary frame; the sealed tail chunk gets the manifest-level
   `reconnect_gap` flag (never written into Raw frames, never a fabricated
   exchange payload). Evidence states `boundary_frame_persisted=false`,
   `boundary_kind=no_last_frame_available`, and records the disconnect
   transport time instead of a payload hash.
2. **The durable reconnect intent is persisted before the old generation
   seals** (INV-007): a crash during or before the seal leaves the Catalog
   STARTED behind, and startup recovery re-seals the old partial fail-closed.
   A gap may only be completed by a frame whose connection_id differs from
   the gap's `original_connection_id`.
3. **A connection that fails before delivering any frame extends the pending
   gap**: no second STARTED, no generation bump, no nested gap. The gap
   completes only after the first reliable new-generation Raw frame is
   persisted and synced.
4. **diff_depth never reconnects inside a capture session.** Any boundary
   retires the session; the outer loop restarts with a fresh connection,
   fresh REST Snapshot, and correct U/u/pu bridging before READY. Raw gap
   evidence is independent of orderbook recovery.
5. **Session restart (depth-resync-triggered) is a reconnect boundary for
   every stream** whose connection closes and reopens; a true global stop is
   not.
6. **`server_shutdown` and `sequence_gap` coexist**: the shutdown frame keeps
   its `server_shutdown` flag in Raw; the boundary additionally carries
   `reconnect_gap` at manifest level and a STARTED/COMPLETED pair.
7. **Crash recovery**: a unique unmatched STARTED (any reason) is restored
   with the same gap_id and generation; conflicting gaps fail closed.
8. **Writer/seal/sync/Catalog failures remain process-fatal** under the
   M21.4.2 owned-blocking-worker model; no new connection opens after a
   failed generation seal.

## 8. Manifest defense in depth [DERIVED]

`seal_partial()` now fails closed: a chunk containing more than one
connection_id whose connection transitions lack boundary-local evidence is
sealed with forced `reconnect_gap` (`gap=true`, `complete=false`). Blue/green
overlap is safe only when it covers the exact transition (both boundary
frames carry the overlap flag with a shared deployment identity); a lone
overlap flag elsewhere in the chunk never exempts an unrelated transition
(M21.4.11-R2). Legacy partials recovered at startup go through the same
defense, and startup recovery derives forced flags from durable open-gap
state (M21.4.11-R1).

## 9. Historical remediation (design only; not executed) [DERIVED]

Historical sealed Raw/Manifest evidence is immutable and must not be
rewritten. Options to make the corrected judgment visible to consumers:

| Option | immutability | determinism | replay | normalization | consumer visibility | idempotence | auditability |
|---|---|---|---|---|---|---|---|
| A. Catalog additive integrity-correction events | preserved (new events only) | high | requires new replay queries | requires new normalization input | high (Catalog is the state plane) | yes (event_id keys) | high |
| B. Versioned superseding quality index (new artifact family) | preserved | high | additional artifact | additional artifact | high | yes | high |
| C. Replay-time reconnect-boundary inference | preserved | high (deterministic from connection_ids) | built-in | no Raw change | visible at replay time only | n/a | medium |
| D. No correction (keep historical claims) | preserved | n/a | n/a | n/a | misleading complete=true persists | n/a | low |

**Recommendation: A (+C as a replay-time safeguard).** The Catalog
operational-events table already carries the gap vocabulary; additive
`INTEGRITY_CORRECTION_STARTED/COMPLETED` style events (or a versioned
superseding index keyed by chunk_id) can mark the 4,680 unmarked intervals
without touching sealed artifacts. Any migration must be additive,
versioned, auditable, idempotent, and covered by tests before execution.

**POST_MERGE_MIGRATION_REQUIRED=true** (design documented; execution
explicitly deferred to a separately reviewed follow-up after this PR merges).

## 10. Tests added [OBSERVED]

`tests/integration/test_reconnect_boundary_integrity.py` (31 tests):
unexpected_disconnect book_ticker/agg_trade; planned_rotation
book_ticker/agg_trade; diff_depth unexpected + planned rotation session
retirement; repeated connect failures before first-new frame (one gap_id,
one STARTED, one generation transition, one COMPLETED); crash after STARTED
before first-new (restart recovery, no false complete); COMPLETED write
failure after first-new (fatal, recoverable, Raw marker persists); writer
seal failure during generation (fatal, no new connection); graceful shutdown
(no false gap); session-restart boundary (gap recorded; global stop is not);
blue/green overlap preserved; unmarked multi-connection seal defense;
legacy multi-connection partial recovery defense. M21.4.11-R1 crash matrix
(TEST-101..111): crash after durable STARTED before seal; crash during seal
after Catalog SEALING; sealed artifact before manifest; manifest before
Catalog SEALED; old generation sealed before replacement opens; first-new
append before Raw sync; restart after COMPLETED (no duplicate evidence);
book_ticker/agg_trade parametrization; Spot crash recovery; diff_depth
crash/restart session retirement; existing-manifest semantic conflict
fail-closed (REQ-107); overlap-at-one-transition defense (TEST-601).
M21.4.11-R1 review follow-up: three deterministic drain-completion race
tests (USD-M book_ticker, USD-M agg_trade, Spot book_ticker) hold the
writer drain until the boundary decision has run, then release it; each
asserts the post-drain STARTED, the same-gap COMPLETED pair, and the
replacement generation's marked first frame, and fails without the
post-drain intent record.

`tests/unit/test_historical_reconnect_audit.py` (24 tests): boundary-local
intra/inter-chunk classification, single-frame marker ambiguity, archived
no-evidence boundaries, Catalog gap identity matching (stream-specific,
nearby-gap selection), gap_id-exact unmatched counts, read-only semantics
(no directory creation, no tree mutation, read-only mounts), output target
rejection (inside root, symlink), cutoff/inventory determinism, canonical
SHA reproducibility.

`tests/integration/test_usdm_side_data_transport_integrity.py` (3 tests):
mark_price and liquidation terminal storage failure fails closed (no
replacement opener, FAILED status, core stop unset); network disconnect
recovers inside the collector's Reconnect Boundary logic without supervisor
restart.

`tests/unit/test_historical_reconnect_audit.py` (7 tests): deterministic
JSON; classification of explicit/overlap/unmarked fixtures (legacy
gap=false/complete=true manifests emulated); Catalog interval correlation;
REST-stream exclusion; corrupt artifact; missing sealed file (manifest-level
transition); malformed manifest tolerance; market/stream filters.

Existing M21.4 backpressure tests are unchanged in contract and still pass;
diff_depth stream tests were updated to the new contract (depth retires its
session at a boundary instead of reconnecting in place), with assertions
strengthened to gap/complete/sequence_gap evidence.

## 11. Validation [OBSERVED]

- pytest (unit+integration, offline): `569 passed, 4 skipped, 25 deselected`
  (initial head); `657 passed, 28 skipped, 4 deselected` (M21.4.11-R1..R5
  correction); `661 passed, 28 skipped, 4 deselected` (post-review
  drain-completion race follow-up)
- stress (1000 reconnect/rotation cycles, 100 backpressure boundaries,
  million-event): `3 passed` (cycle test now seals each of its 1,000
  generations; observed ~11-52s)
- Ruff: PASS; strict MyPy: PASS (198 source files); M0 contracts: PASS;
  Go raw-chunk golden: PASS; sdist/Wheel build: PASS (dev Wheel SHA
  `7035e077...`, not deployed); clean wheel install + CLI smoke
  (version/doctor/status): PASS
- `git diff --check`: PASS (run before commit)
- Corrected read-only production audit independently re-run with the same
  cutoff `1786349202047196027`: canonical payload byte-identical across the
  three runs (SHA-256 `7143bc0cd3370c831df773a5bd9246d86ee95377beb57e08097519a9c8a520b3`),
  inventory `161,817` manifests (`ffaf34bd...`), 4,691 transitions (11
  explicit, 4,680 unmarked, 0 ambiguous), Catalog 11/11/0/0 by gap_id.
  Before/after `find -xdev` inventory: only the running recorder's own
  writes (new partials/manifests/sealed, catalog.sqlite and
  service_state.json updates); no pre-existing sealed/manifest/Raw file was
  modified and no directory was created.

CI: Ubuntu and macOS GitHub Actions runs are attached to the PR.

## 12. Production status [OBSERVED]

- MainPID 317289, NRestarts=0, service active; Spot/USD-M READY, both
  orderbooks synchronized; archive backlog healthy (160,483 transactions,
  all LOCAL_DELETED after verified archival); storage OK.
- Wheel `926615b09...` (commit cf1e749c) remains installed. **Nothing was
  deployed, restarted, or modified by this task.**
- Current runtime health is not evidence of data-integrity qualification:
  the running artifact still seals unmarked reconnect boundaries as
  `complete=true`. Until the forward fix is reviewed, merged, and deployed,
  any interval crossing a reconnect boundary produced by the current
  artifact must not be trusted as complete.

## 13. New artifact validation chain [DERIVED]

Once this PR is reviewed, merged, and a separately authorized deployment
happens, the old artifact's 2h/12h/24h/72h evidence is historical reference
only. The new artifact must re-execute the full chain: deployment canonical
identity -> readiness -> 2h -> 12h -> 24h -> 72h -> 168h. The repair being
small does not waive the long windows. This document does not start any
window.

## 14. Production Ready [DERIVED]

**Not Production Ready.** FORMAL_72H_RESULT=FAIL, 168h not started, and the
forward fix is not deployed.
